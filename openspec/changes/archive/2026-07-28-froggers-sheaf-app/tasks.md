## 0. Ground rules (apply to every packet)

- Sections **1→12** are sequential for code changes. No parallel implementer subagents until the lead publishes a dependency map the operator accepts (OMNI §4). Explore-only subagents may parallelize.
- Every subagent runs on the least capable sufficient model — **Sonnet or Haiku, never the top model** (OMNI §4/§15, operator standing rule).
- Builds are capped at **`-j2` wrapped in `nice`** (8-core/16 GB host freezes otherwise). Verification runs go through a subagent that reports pass/fail counts + failure tail only (OMNI §16.1).
- **Frozen trees — zero edits:** `desktop-v2/`, `desktop/`, `src/`, `sim/`, `wasm/`, `vcv/`, `web/`.
- **Permitted files outside the new app tree** (the scope gate allows these and nothing else): `.gitmodules`, `External/Sheaf` (gitlink), and `.github/workflows/pages.yml`. The root `Makefile` is **not** permitted — it is the Daisy firmware's entry point (`include src/mk/config.mk`). Every packet ends with a scope check proving the diff touched only the new app tree plus this list.
- **RESOLVED 2026-07-27 (late) — the fork was reverted; the pristine-pin property HOLDS.** For part of 2026-07-27 the submodule sat on a local branch `froggers-fork`, two commits ahead of the pin (`04818deb`, `7fa9ce34`, both in `projects/synth/juce/PortableJuceBackend.hpp`), adding plain-click dispatch for Draw nodes. Those commits existed only on one machine, so the gitlink was unresolvable from any other checkout and the D12 browser publish was blocked. **That is over.** The submodule is back on upstream pin `1940ddcb`, confirmed reachable on `origin/main` of `jvictor0/Sheaf`; `git diff <baseline> --name-status` shows `M External/Sheaf` solely as the sanctioned `dafa54b6 → 1940ddcb` pin bump from task 1.1. The reverted commits are preserved on the local `froggers-fork` branch for upstreaming by jvictor0.
  - **App-side cost of the revert:** encoder press and Play/Stop are **double-click**, since `Node::doubleClickAction` is the only action hook stock Sheaf dispatches for Draw nodes at this pin. `app/FroggersUiSurface.hpp`'s `SetNodeAction`/`SetNodeActionAndLabel` and the assertion in `app/FroggersSurfaceTests.cpp` all flip back to `node.action` in one edit each once plain-click lands upstream.
- **No file inside `External/Sheaf` is edited by this change.** Sheaf's out-of-tree launcher-registration hook (design D1a) is entirely upstream, arriving via the pin bump to `1940ddcb` (task 1.1); the only change to the submodule's presence in this repo is the gitlink itself moving.
- **Superseded ≠ deleted.** This change makes `desktop-v2/`, `wasm/` and `web/` redundant but deletes none of them; they stop being built. Deletion, and the removal of the `src/core/` V2 gates, are follow-on changes (see proposal's Retirement path). Nothing here may anticipate them.
- The new app must never include from `desktop-v2/External/Sheaf` (the frozen vendored slice, a plain vendored directory — not a submodule). Only `External/Sheaf`.
- **Sheaf citation paths** in this document are relative to `External/Sheaf/projects/synth/`. The browser tree is `projects/synth/browser/`; `External/Sheaf/browser/` does not exist.
- **Slot indices are 0-based** (design D5a): a 16-slot bank is `0..15`, Crispy is index **14**, Crunchy index **15**.

## 1. Baseline, submodule + build skeleton

- [x] 1.0 **Record the frozen-tree baseline commit.** Baseline is **`b409106`**. Working tree is clean as of 2026-07-25 (only untracked `.claude/`, `.cursor/`). Pre-baseline cleanup already done: `66e3903` refreshed the 5 tracked Daisy build artifacts under `src/FroggersTiga/build/`, and `b409106` removed the dead Sheaf param-bridge spike from desktop-v2. Task 12.4 compares against `b409106`, never against a dirty tree.
- [x] 1.1 Bump `External/Sheaf` submodule pin to **`1940ddcb`**, superseding `dafa54b6` (already present via commit `efb0f8b`), to pick up Sheaf's out-of-tree app-registration hook (design D1a). The gitlink move is the **only** permitted change outside the new app tree for this task — no file inside `External/Sheaf` is edited. Record the pin in the change and in the app's build docs.
- [x] 1.2 New app tree + a **Makefile** (not CMake — design D1b) modelled on `apps/miniapp/Makefile`, linking Sheaf's synth core (`libsynth.a`) directly; compiles an empty translation unit against Sheaf headers. Packet 1 needs no JUCE — nothing in it touches the `sheaf-patch` launcher or any JUCE header — so this Makefile does not include Sheaf's `runtime/juce_build.mk`. That machinery enters in **packet 2**, where the `sheaf-patch` launcher build (task 2.2, design D1a) genuinely requires JUCE.
- [x] 1.3 Include-path guard, for a Makefile build: a build- or test-time check that no include path the new target resolves against reaches into `desktop-v2/External/Sheaf` — e.g. asserting the compiler's effective include search path (or grepping the Makefile's `-I` flags) contains no `desktop-v2/External/Sheaf` entry, so the build fails if it ever appears.
- [x] 1.4 Scope gate: diff touches only the new tree + the permitted list in §0.

## 2. `FroggersApp` under Runtime

- [x] 2.1 `FroggersApp` satisfying `synth::SynthApplication` — `Config()`, `Init(context)`, `ProcessBlock(block)` come from `SynthApplicationCore` (`include/synth/AppConcepts.hpp:12-16`), `PortableSurface()` is added by `SynthApplication` (`:19-22`); `static_assert` **`SynthApplication`** (the full concept).
- [x] 2.2 **Register into the `sheaf-patch` launcher via the out-of-tree hook (design D1a).** Provide `FroggersRegistration.hpp` with a manifest factory and `MakeFroggersRegistration(LaunchFn&&)`, modelled on `apps/braid-4/Braid4Registration.hpp:20-23`; `appId` is `frogg3rs`. Build the launcher with the five `EXTRA_APP_*` Makefile variables, all mandatory together:
  ```
  make -C External/Sheaf/projects/synth/apps/sheaf-patch \
    EXTRA_APP_DIR=$PWD/app EXTRA_APP_HEADER=FroggersRegistration.hpp \
    EXTRA_APP_TYPE=synth_froggers::FroggersApp \
    EXTRA_APP_REGISTRAR=synth_froggers::MakeFroggersRegistration \
    EXTRA_APP_HEADERS="$(ls $PWD/app/*.hpp $PWD/app/dsp/*.hpp)"
  ```
  **CORRECTED 2026-07-27.** The example above previously listed only two headers, which was not merely stale but actively dangerous: the app has 18 headers, `APP_HEADERS` is a literal prerequisite list (`Makefile:47-48`), and neither the Makefile nor `runtime/juce_build.mk` emits `-MMD`/`-MP` dependency files. The 14 unlisted headers — **all of `app/dsp/` among them** — were untracked, so edits to them produced builds that succeeded while silently ignoring the change. Use `app/build-launcher.sh`, which globs, instead of any hand-written list.
  Partial configuration (any one of the five variables set without the rest) is a hard `$(error)` in the Makefile — verify this. `EXTRA_APP_HEADERS` must list our own headers so edits to them trigger a rebuild; note the trap upstream caught, where an `APP_HEADERS +=` placed before the `APP_HEADERS :=` assignment is silently clobbered, producing a build that succeeds while ignoring changes to our headers. **This task also owns bringing in Sheaf's `runtime/juce_build.mk`** — packet 1's Makefile (task 1.2) links only `libsynth.a` and deliberately has no JUCE dependency; the `sheaf-patch` launcher build above is the first point in this change that genuinely requires JUCE.
- [x] 2.3 Headless test: `Init` → `ProcessBlock` produces finite stereo output.
- [x] 2.4 Mechanically enforce "no JUCE in the app core": a build- or test-time check that no app-core translation unit resolves a JUCE header (spec `froggers-sheaf-runtime-app` requires this, not just the absence of a link dependency).
- [x] 2.5 **Validate the mono assumption end-to-end (do this before packet 4).** The design's earlier "mono is first-class" claim was overclaimed — its citations (`Modules.hpp:298-300,765`) are module-local guards, and `StandardModulators` special-cases mono *out* (`:143,312`). Stand up one `ParameterGroup{numVoices=1}` with a bank, a modulator, and a depth cell, and confirm: parameters register and resolve; `PopulateUIState` publishes a single voice; `EncoderDrawStateFromParameter` yields `voiceCount == 1` and renders one ring, not a degenerate stack; drill-in materializes depth params. **If any of this is second-class, stop and ask** — the bank model, slate, and surface all depend on it.
- [x] 2.6 **`Config().numAudioInputs` is currently 0** (`app/Froggers.hpp:72`), which makes the modulation slate's two external-audio sources (design D5, slots 13-14: audio rate and its envelope follower) permanently `.connected = false` by construction — not because no cable is present, but because the app never asks the host for an input channel at all. Raise `numAudioInputs` from 0 to the channel count those two sources require (both lanes are derived from the same external signal per the D5 slate table, so this is expected to be 1, but confirm against the host's input-channel contract before hardcoding it). Test: with an input present, both external-audio sources can report `connected == true`; with none, both report `connected == false` — i.e., the flag now tracks actual cabling, not a permanently-zero input count.

## 3. DSP port (copy, pure, tested)

Copy as dependency-free translation units; each gets a parity unit test pinning it to the cited Froggers formula. No `#include` of `src/`, `sim/`, or `desktop-v2/`.

- [x] 3.1 VCO: pitch exp map 20 Hz–20 kHz (`src/core/FroggersEngine.hpp:439-441`), `EvalWaveMorph` sine→saw→square (`src/core/VcoWaveEval.hpp:7-23`), independent PM sine LFO `0.05·(20/0.05)^knob` (`FroggersEngine.hpp:706-712`, constants `:135-137`). **The `0.15 · PmDepthScale(knob)` depth multiply lives at the caller** (`:741-743`), not inside `StepIndependentPmLfo`; `PmDepthScale` is the 0.02→0.10 smoothstep (`:150-165`, thresholds `:147-148`). **Zero cross-VCO terms** — port the independent-PM branch (`:735-744`), not the legacy `else` branch (`:752-754`), which does use cross terms.
- [x] 3.2 ASR envelope + voice mix (`FroggersEngine.hpp:772-809`; `src/core/VcoAdsrState.hpp`). Port the **`m_vcoAdsr` branch** (`:774`); the `m_pairAr` fallback (`:786-788`) is v1 legacy and is **not** carried over.
- [x] 3.3 Envelope followers: `attack 0.01 s / release 0.05 s`, `coeff = 1 - exp(-1/(t·sr))` (`sim/V2EnvelopeFollowerBank.hpp:22-25`), taps at `:30-35`. Sheaf ships no parameterizable follower (its `Meter`, `include/synth/DspMetering.hpp:19-53`, has fixed alphas and is a metering utility). Only taps 3, 4 and 5 (`|v1|`, `|v2|`, `|v3|`) feed the slate. The two pair taps at indices 6 and 7 (`|v1+v2|·0.5`, `|v2+v3|·0.5`) are **superseded by D5's slate** and must NOT be ported — porting them would create modulation sources nothing consumes.
- [x] 3.4 **Six Random S&H sources, no source-level controls (D8, revised).**
  - `src/core/Marbles.hpp` is **hardcoded to exactly two channels** and is not a drop-in six-source unit: `m_marbles[2][8]` (`:12-13`), `m_index[2]` (`:17`), plus `m_filter[2]`, `m_size[2]`, `m_dejaVuKnob[2]`, `m_output[2]`, and every loop is `for (i = 0; i < 2; ++i)`. `m_probability` is a **single shared scalar**, not per-channel, and both channels share **one** `RGen m_rgen` — so two lanes are not statistically independent today.
  - Six independent sources therefore require either three instances (each seeded distinctly, or their loops correlate) or generalizing the hardcoded 2. Decide and record which, here in packet 3.
  - Strip the `m_page->GetParam()` coupling (`:81-87`, seven reads: Step chance, Deja vu 1, Bag size 1, Slew 1, Deja vu 2, Bag size 2, Slew 2) and the fixed mod-tap wiring to `m_modMgr.m_mods[5]`/`[6]`.
  - **Spread and bias are new DSP, not ported** — `src/core/Marbles.hpp` reads neither and implements no range narrowing or centring, so D8a's "narrow, centred" character for source #3 must be written from scratch.
  - Only sources 1–5 come from this port. **Source #6 is a Sheaf `GangedRandomLfo`, not a Marbles instance** — see design D8 and task 8.1.
  - Bag/deja-vu core is `:90-119` (deja-vu branch `if (0.5 < m_dejaVuKnob[i])` at `:99`); the slew is `OPLowPassFilter` in `Process()` at `:138-144`. Labels remain `Random S&H 1`–`Random S&H 6`; the rate/loop table is design D8a.
- [x] 3.5 Fuegoize + the `V2FuegoStack` cascade (`sim/V2FuegoStack.hpp:9-23`; `ApplyGlobal` is `:9-12`, `ApplyMusicalRow` is `:14-23`) — transform only; application point comes in §5.
  - **Parity reference is the Daisy firmware's `src/core/Parameter.hpp:129-151`, NOT `sim/Fuegoize.hpp`.** `sim/Fuegoize.hpp:23` casts the modulo's *divisor* to `uint8_t`, so at knob ≥ 0.9375 (`mask = 255`) it evaluates `row % 0` — UBSan-confirmed undefined behavior that returns different values at `-O0` and `-O2`. `Parameter.hpp:143` casts the *result* and is correct.
  - Add a regression test at knob `1.0` (and `0.9375`) proving the copy is finite, deterministic, and equal to the firmware formula `sh = 1 + (row % 256)`.
- [x] 3.6 `ResonantBump` (RBJ peaking biquad, `src/core/ResonantBump.hpp:44-71`), `Comb` (`src/core/Comb.hpp:54,63,66-76`), `PureDelay` (`:79-109`), and the serial/parallel FX routing (`FroggersEngine.hpp:826-829,836-838`). The comb's saturator is a **Padé rational approximation**, not `std::tanh` (`src/core/TanhSaturator.hpp:25-30`, Padé formula at `:28`) — pin the approximation, including the **clamp** at `:29`.
- [x] 3.7 Parity test suite green; scope gate.
- [x] 3.8 **Reverb DSP for the Reverb page's 9 params** (Wet/dry, Room size, Decay, Pre-delay, Damping, Stereo width, Diffusion, Mod depth, Hold) — extended port scope, design D15. Port `ProcessReverb` (`src/core/FroggersEngine.hpp:493-534`, param wiring `:455-461`) for the 7 params it already implements: Wet/dry (`m_rvMix`), Room size (`m_rvSize`), Decay (`m_rvDecay`), Pre-delay (`m_rvPre`), Damping (`m_rvDamp`/`m_rvDampFilter`), Stereo width (`m_rvWidth`), Diffusion (`m_rvDiffusion`). **Mod depth and Hold have no Froggers original** — `m_reverbParams->GetParam(7)`/`GetParam(8)` are never read anywhere in `FroggersEngine.hpp` — so these two are newly authored, not ported, the same way D8 treats spread/bias for the Random S&H sources. Parity tests pin the 7 ported params against `ProcessReverb`'s formulas at fixed knob settings; Mod depth and Hold get behavioral tests only (no frozen reference to pin against). **Done:** ported in `app/dsp/Reverb.hpp` (`Reverb::Process`, exposing `RoomSizeFromKnob`/`DecayFeedbackFromKnob`/`PreDelayNormFromKnob`/`DampAlphaFromKnob` statics for the 4 ExpMap-derived params, plus direct passthrough for Wet/dry, Stereo width, Diffusion, and the `:844-846` mix blend folded into `Process`'s return). Mod depth (tank-tap wow) and Hold (feedback pushed toward, never reaching, 1.0) are authored, clearly commented as such, and are no-ops at their neutral defaults so the 7 ported params reproduce `ProcessReverb` exactly. 7 new parity/behavioral tests added to `app/FroggersDspParityTests.cpp`, all green (see report).
- [x] 3.9 **Drive/distortion DSP for the Drive page's 9 params** (Drive, Shape — a wavefolder, a *different* parameter from the Audio bank's Shape VCO morphs of task 3.1 — SRR 1, SRR 2, XOR, Bit depth, Fuzz, Blend, Phase) — extended port scope, design D15. Port `FrogBlock` (`src/core/PolynomialDrive.hpp`: `PolynomialDrive`, `TanhSaturator`, `SampleRateReducer`, `DigitalReorganizer`, `Oversampler2x`; wiring `src/core/FroggersEngine.hpp:81-92,207,483-490,569-573,640-650`) for the 7 params it already implements: Drive (`PolynomialDrive::SetGain`), Shape (`PolynomialDrive::SetCoefs`), SRR 1 / SRR 2 (`m_srr1`/`m_srr2`), XOR (`m_digr` → `DigitalReorganizer::SetFlip`), Bit depth (`m_hash` → `DigitalReorganizer::SetHash`), Fuzz (`m_fuzz`). **Blend and Phase have no Froggers original** — `m_driveParams->GetParam(7)`/`GetParam(8)` are never read anywhere in `FroggersEngine.hpp` — so these two are newly authored, not ported. Parity tests pin the 7 ported params against `FrogBlock`'s formulas; Blend and Phase get behavioral tests only. **Done:** ported in `app/dsp/Drive.hpp` (`PolynomialDrive`, `SampleRateReducer`, `DigitalReorganizer`, `Oversampler2x`, `FrogBlock`); `TanhSaturator<false>`'s fuzz path reuses the already-ported `dsp::PadeSaturator` (FilterFx.hpp) since the frozen `FrogBlock` fixes its input gain at 1.0 for the struct's whole lifetime, making the two formulas identical (documented in the file header). Blend and Phase are authored as `DriveBlendPhase` (dry/wet crossfade + a stable first-order allpass on the wet signal), a no-op passthrough at blend=0. 10 new parity/behavioral tests added, all green.
- [x] 3.10 **Delay DSP for the Delay page's 9 params** (Delay time, Send, Feedback, Stereo width, Detune, Mod depth, Wet mix, Color, Halo) — extended port scope, design D15. **CORRECTED (lead review, 2026-07-26): a full Froggers original exists — port it, do not author one.** An earlier draft of this task claimed eight of the nine params had no frozen original. That was wrong: it searched only `src/core/`. The Delay page is implemented in **`sim/DelayState.hpp`**, absent from `src/core/` because the delay is a sim/desktop feature the Daisy firmware never shipped. **Done:** ported in `app/dsp/Delay.hpp` — `StereoDelay` (from `sim/StereoDelay.hpp`, whole file) and `MapRowsToDelayParams` (the row→`DelayParams` mapping and Color/Halo fold from `sim/DelayState.hpp:180-193`). All nine params ported, nothing authored. Knob smoothing, mod-source/depth routing, the fuego seam, and randomize/sanitize machinery were deliberately NOT ported (owned by Sheaf's parameter model / packet 5), per this task's explicit instruction. 7 new parity tests added, all green.
  - `DelayState::processInsert` (`sim/DelayState.hpp:165-198`) maps **all nine rows**: row 0 → `params.dtim`, 1 → `dsnd`, 2 → `dfbk`, 3 → `dwid`, 4 → `ddet`, 5 → `dmod`, 6 → `dmix` (`:180-186`).
  - **Color and Halo are not independent parameters.** Under `m_useV2Layout` they *bias* two others: `ddet = clamp(0.5*(ddet + color))` and `dmod = clamp(0.5*(dmod + halo))` (`:187-193`). Port that folding exactly — treating them as standalone controls would be wrong.
  - The delay engine itself is **`sim/StereoDelay.hpp`** (`:9`), driven via `delay.process(bumpIn, params)` and `delay.toReverbMono(bumpIn, wet, params.dmix)` (`:196-198`). Port it as a dependency-free unit like the rest of packet 3.
  - The already-ported `PureDelay` (`src/core/Comb.hpp:79-109`) is a *different* unit, driven by the **Filter** page's slot 0 (`FroggersEngine.hpp:463`). Do not conflate the two; the Delay bank uses `StereoDelay`, not `PureDelay`.
  - Parity tests pin all nine rows against `DelayState::processInsert`'s mapping, including the Color/Halo folding, in the same discipline as the rest of packet 3.

## 4. Parameter model (mono, 16-slot banks)

- [x] 4.1 One `ParameterManager`; `ParameterGroupConfig{numVoices=1, numModulators=15, numScenes=…, maxParameters=…}` (`include/synth/ParameterModulation.hpp:195-198`).
- [x] 4.2 Build the six banks per design **D5a** — one bank per v2 page, **no merging**, slots indexed **0–15**: indices 0–8 carry the page's nine parameters, 9–13 empty, **index 14 = local Crispy**, **index 15 = global Crunchy** (fixed in every bank). Audio gains the three **Shape** controls as ordinary slots. Reverb includes its expansion-tail rows `Mod depth` and `Hold`.
  - Derive each page from `desktop-v2/Source/V2DesktopPageDisplayNames.hpp` using `forHostPageRow`'s real logic (`:148-159`): grid rows `< 7`, then the 3-row tail for rows 7–9. **Do not copy `kHostRowGrid` wholesale** — each row array is 8 wide with a trailing dead `"Crispy"` at index 7 that is never read.
  - Registration mechanics: `Bank::RegisterParameters(std::span<Parameter* const>, std::size_t offset)` (`include/synth/ParameterModulation.hpp:611`) takes a **contiguous** run, so each bank needs three calls — nine page parameters at offset 0, Crispy at offset 14, Crunchy at offset 15. Crunchy is **one shared `Parameter`** registered into all six banks; duplicate-name checking is within a single call only (`src/ParameterModulation.cpp:2572-2578`) and there is no `Parameter`→`Bank` back-pointer, so this is structurally expressible — but see the new task 4.6.
  - **Discovered during implementation:** `ParameterManager::RegisterParameter` separately enforces GLOBAL name uniqueness across the whole manager (`src/ParameterModulation.cpp:3069-3071`, not cited above), which is stricter than the per-call check cited here. Two page labels genuinely repeat across banks in D5a's own table (`Stereo width` on Reverb and Delay; `Mod depth` on Reverb and Delay). Resolved by qualifying each page parameter's internal `Name()` with its bank name (`"Reverb Stereo width"` vs `"Delay Stereo width"`) while leaving `ShortName()` — the field the grid actually renders — as the authentic, page-local, still-repeating label. See `app/FroggersParameters.hpp`'s comment at the page-parameter registration loop.
- [x] 4.3 Register all parameters with Froggers value scaling + `ParameterConfig{baseColor}` (`ParameterModulation.hpp:260-269`). `Bank::SetBankColor`/`BankColor()` (`:622-623`) has **zero non-test consumers** in Sheaf and renders nothing — encoder color comes solely from per-parameter `ParameterConfig::baseColor`, read at `include/synth/EncoderDraw.hpp:322` and used at `:670,685,687`. So **one color per bank** means assigning that bank's color to every one of its parameters' `baseColor`; `SetBankColor` is still set as the bank's declared identity but must not be relied on for rendering. Consequence: the shared Crunchy has a single `baseColor` and therefore keeps one fixed global-control color in all six banks.
- [x] 4.4 One shared `BankSlot`; bank select; scenes wired (`SceneState`), **including scene blend** — parameter values interpolate between scene endpoints and the change reaches the published ring state.
- [x] 4.5 Tests: parameter count/identity per bank (9 + Crispy@14 + Crunchy@15); Crispy/Crunchy slot identity is stable across bank changes; per-bank color; scene blend interpolates across banks; scope gate.
- [x] 4.6 **Validate the shared-Crunchy assumption before packet 5 depends on it.** Register one `Parameter` at slot 15 in two or more banks and confirm: both banks resolve it; editing it from either bank moves the same value; `PopulateUIState` publishes one parameter, not two; drill-in from either bank targets the same parameter; and no duplicate-registration or bank-ownership assertion fires. Nothing in Sheaf demonstrates a parameter shared across banks. **If it is second-class, stop and ask** — this is the same class of assumption as mono (task 2.5). **Gate PASSED** — all checks confirmed with all six banks in `app/FroggersParameterModelTests.cpp`'s `shared_crunchy_resolves_and_moves_identically_from_any_bank`; no assertion fired.

## 5. Fuegoization on the filter seam

- [x] 5.1 Fuego row identity = the parameter's **0-based slot index** in its 16-slot bank (D6, resolved); `crispyRow` = **14** in every bank (`sim/V2FuegoStack.hpp:18,21`). No legacy row-id carry-over. Implemented in `app/FroggersParameters.hpp`'s `ApplyFuegoSeam()`: page params use `paramIx` (0-8) as `row`, Crispy uses `kFroggersCrispySlot` (14).
- [x] 5.2 Apply global Crunchy → per-bank Crispy inside `ProcessLitePhase1()` → `ReplaceCachedKnobValue()` → `ProcessLitePhase2()` (`ParameterModulation.hpp:485-491`), following Braid's per-oscillator filtering structure (`apps/braid-4/Braid4Core.hpp:569-627`, call sequence `:457-459`) and its seeding discipline (`:629-667`).
  - **API used, and why no switch from packet 4 was needed:** Braid's actual call sequence is at the `ParameterGroup` level — `ProcessParameterPhase1()`/`ProcessParameterPhase2()` (`Braid4Core.hpp:565-573`) call `group->ProcessSamplePhase1(internalIndex)`/`group->ProcessSamplePhase2()`, **not** `Parameter::ProcessLitePhase1/2` directly; `FilterParameterCaches()` sits between them and does its work via `Parameter::CachedKnobValue()`/`ReplaceCachedKnobValue()` (`Braid4Core.hpp:592-627`). `Parameter::ProcessSamplePhase1(sampleIndex)` is itself a thin wrapper (conditional `Compute()` + `ProcessLitePhase1()`, `ParameterModulation.cpp:1486-1490`) and `ProcessSamplePhase2()` wraps `ProcessLitePhase2()` (`:1493-1495`) — so the two API families expose the identical seam at two layers. Packet 4's choice of `ParameterGroup::ProcessSamplePhase1/2` (`FroggersParameterModel::ProcessSample`) was already the correct layer; no switch was needed, only inserting the filtering step between the two calls that packet 4 had already wired back-to-back. Added `FroggersParameterModel::ApplyFuegoSeam()` (`app/FroggersParameters.hpp`), called once between `group_->ProcessSamplePhase1(sampleIndex)` and `group_->ProcessSamplePhase2()` in `ProcessSample()`.
  - **"Exactly one application point," how verified:** `ApplyFuegoSeam()` is one private method with one call site (`ProcessSample()`); `grep -rn "ReplaceCachedKnobValue" app/` (excluding `build/`) shows exactly two call sites, both inside `ApplyFuegoSeam()` (one for each bank's Crispy, one for each bank's page params) — mirroring Braid's own `FilterParameterCaches()`, which likewise calls `ReplaceCachedKnobValue` many times from one seam function, not literally once. Uses the ported `app/dsp/Fuegoize.hpp`'s `FuegoStack::ApplyGlobal`/`ApplyMusicalRow` (packet 3, already tested), not a new copy of the cascade.
- [x] 5.3 A Crispy control receives **only** the global stage; Crispy is itself Crunchy-warped before use (`sim/V2FuegoStack.hpp:9-23`; the v2 wiring that proves the rule is `src/core/Page.hpp:203-207`). Verified `src/core/Page.hpp:203-207` is exactly the `position == m_v2CrispyRow` branch calling only `applyGlobal` (global stage), with the else-branch (`:208`) taking the full musical cascade — citation checks out. Implemented in `ApplyFuegoSeam()`: `crispy.ReplaceCachedKnobValue(kVoiceIx, FuegoStack::ApplyGlobal(crispyPreFuego, globalCrunchy, crispyRow))`.
- [x] 5.4 Knob rendering: **processed value only**, matching Braid (D9a, resolved) — no raw ghost tick. No app-level draw/encoder code exists yet (packet 10's scope); `grep -rn "rawKnobValue" app/` (excluding `build/`) returns nothing, so there is nothing in `app/**` that could read it. The seam's existence alone (per D9a's traced chain) means `UIDisplayCenter` — the only thing `EncoderDrawStateFromParameter` reads — already chases the fuegoized cached value once packet 10 wires the draw path.
- [x] 5.5 Tests (model level — the rendered-surface assertion moves to 10.5): fuego transform reaches the DSP; a Crispy sweep changes sibling parameters on its own bank only; global Crunchy affects all banks; the value published to `UIState.values[v]` is the fuegoized one. Note `PopulateUIState` is throttled by `uiPublishInterval_` (`include/synth/Engine.hpp:409`) — pump enough blocks. Five new tests added to `app/FroggersParameterModelTests.cpp`: `fuego_seam_transform_reaches_cached_knob_value_matching_dsp_stack`, `crispy_sweep_changes_sibling_parameters_on_its_own_bank_only`, `global_crunchy_affects_all_banks`, `global_crunchy_itself_receives_no_fuego_stage` (5.6's test), `fuegoized_value_is_published_to_ui_state_values`. All pass.
- [x] 5.6 **Global Crunchy itself receives no fuego stage.** The cascade defines ordinary parameters (both stages) and Crispy (global stage only, `src/core/Page.hpp:203-207`), but D5a newly puts Crunchy on the grid at slot 15 and warping Crunchy by Crunchy is self-referential. Rule: Crunchy passes through unwarped. Add a test. **Operator-confirmed (2026-07-27).** Implemented: `ApplyFuegoSeam()` never calls `ReplaceCachedKnobValue()` on the shared Crunchy parameter (code comment at the call site restates the confirmed status verbatim). Test: `global_crunchy_itself_receives_no_fuego_stage` asserts `crunchy.CachedKnobValue(0) == crunchy.GetRaw(0)` exactly after processing with a nonzero, non-edge Crunchy value.

## 6. Modulation slate + drill-in

- [x] 6.1 **Do not use `StandardModulators`** (D5, revised). `kRandomCount = 4` is a hard `static constexpr` (`include/synth/StandardModulators.hpp:27`) with four hardcoded visualizers (`:46-49,188-191`), so it cannot yield six S&Hs; and with slots 0-5 taken there is nowhere to put its four randoms. Register all 15 sources directly via `Modulators::SetModulationSource` (`src/ParameterModulation.cpp:552-572`). Reuse the standalone pieces: `NoiseModulatorProcessor` (`include/synth/DspNoise.hpp:48-54`), `NoiseWaveformVisualizer` (`:14`), `GangedRandomLfoVisualizer` (`include/synth/GangedRandomLfoVisualizer.hpp:247-248`).
- [x] 6.2 Register the slate in design D5's order: **Random S&H 1–6 (0-5)**, VCO 1/2/3 audio (6-8), VCO 1/2/3 EF (9-11), Noise (12), external audio rate (13), external audio EF (14). External audio sits last because it is the only pair that can go dead. `SetModulationSource` has no reservation registry and will silently overwrite a claimed index, so **registration order is load-bearing** and 6.6 must assert all 15 by identity, not by count.
- [x] 6.3 Source colors + `ModulatorMetadata::sourceColor` (`ParameterModulation.hpp:250`) carried from the v2 palette where sources survive.
- [x] 6.4 **Drill-in depth (D5, resolved).** Sheaf ships **no level model**: `Bank` holds one `Parameter* selected_` (`ParameterModulation.hpp:656`) and one bool (`:673`), `Bank::HandlePress` opens a view for any non-null cell that isn't `selected_` (`src/ParameterModulation.cpp:2647-2648`), and `OpenModulationView` is **private** (`ParameterModulation.hpp:646`). Therefore:
  - Levels 1 and 2 need **no app code** — they are Sheaf's native behavior.
  - Implement an **app-side level counter** in the press routing that does not dispatch the press which would open level 3.
  - Back exits to the parameter grid from **any** level (native `Deselect()`, `:2643-2644`). No one-level pop is implemented.
  - The 16th cell holds the selected parameter itself (`:2854-2857`); pressing it is what triggers `Deselect()`. This is the Target/Back affordance.
- [x] 6.5 External audio with no input: set `ModulatorMetadata::connected = false` (resolved). This is Sheaf's built-in inert state and needs no bespoke affordance — the cell is still pushed with a null parameter (`src/ParameterModulation.cpp:2843-2852`, null from `:2784-2786`) so the slate stays 15 and positions never shift; randomization skips it (`:2889,2898`); it renders as a disconnected encoder via the default draw state (`EncoderDraw.hpp:364`). Flip `connected` back to true when an input appears.
- [x] 6.6 Tests: 15 sources present in order; depth cells materialize; a third-level drill-in is refused; Back returns to the parameter grid; external-audio cells remain present and inert with no input.
- [x] 6.7 **Sheaf remains the only randomization mutator; the app now owns target-set selection (D14, operator 2026-07-26).** `Bank::RandomizeModulationDepths` (`src/ParameterModulation.cpp:2881-2913`) via `Modifier::RandomMod` (`:2875-2876`), plus `Modifier::Random`/`Reset` for a parameter's own value (`:2869-2873`), remain the only mutators — the app introduces no parallel randomization mechanism of its own. What is no longer purely resolved: **which** parameters get handed to those mutators is now an app-side decision (D14), implemented in 6.8 (Randomize All) and 6.9 (Randomize Page). Eligibility is `source.connected` only, which is exactly why 6.5 uses `connected = false` — **no `randomizable` flag is needed**. Test: with no external input, neither external-audio source receives randomized depth.
- [x] 6.8 **Randomize All (D14, context-sensitive by view).**
  - **Pressed on a parameter page (the global case):** iterate every top-level parameter across all six banks: apply `Modifier::Random` (`src/ParameterModulation.cpp:2872-2873`) to its value, and `Bank::RandomizeModulationDepths` (`:2881-2913`) to its **level-1** depths. **Include the six per-bank Crispy controls; exclude global Crunchy.** Never descend to level 2 from here. Materialization ceiling is 915 L1 depth parameters (793 with external audio disconnected).
  - **Pressed on a level-1 mod-detail grid:** apply `Bank::RandomizeModulationDepths` to that one parameter's 15 depths, **then materialize and randomize their level-2 depths** (another `RandomizeModulationDepths` call per now-materialized L1 depth parameter). Total scope for this press is 15 + (15 × 15) = **240** parameters for that one focused parameter — bounded, and the only path in the design that creates L2 storage.
  - **Pressed at level 2:** there is no deeper level (cap is 2, D5), so this coincides with Randomize Page (6.9) — one `RandomizeModulationDepths` call on that depth parameter's own 15 depths.
  - In every case, the app must implement no mutator of its own — it only chooses which parameters to hand to Sheaf and, now, which view-dependent target set to select. `EnsureModulationDepthParameter` returns `nullptr` once `CanAllocate()` fails (`:2790-2792`); on **both** the parameter-page path and the L1-grid path, the implementation must detect and report a partial randomize rather than failing silently — the L1-grid path is the one that can hit this while *creating* L2 storage, so surfacing it there is not optional.
- [x] 6.9 **Randomize Page (D14).** One uniform rule: randomize exactly what is displayed. Parameter page → that bank's nine values plus its Crispy, excluding Crunchy, no depths. Level-1 mod-detail grid → one `RandomizeModulationDepths` call on the selected parameter. Level-2 depth grid → one `RandomizeModulationDepths` call on that depth parameter. Randomize Page always reaches level 2 when pressed there or below; it is no longer the *only* route to level 2, since Randomize All pressed on a level-1 grid (6.8) also reaches it — but Randomize Page is still the only affordance that reaches L2 **without** creating new L2 storage beyond what's already materialized, and the only one that acts identically regardless of view.
- [x] 6.10 **Document the bias in code (D14).** The implementation carries a comment stating: the per-call count is geometric, P(k) = 0.5^(k+1), mean 1.0, from `while (manager_->NextRandomCoin() < 0.5f)` at `src/ParameterModulation.cpp:2894`; that this constant is **Sheaf's**, so changing the per-call bias is an upstream change; and that the app-side knob for aggregate reach is Randomize All's target-set selection (6.8). A future maintainer who wants more or fewer depth changes must be able to read this comment and know which of the two knobs to turn.
- [x] 6.11 **Randomize tests (D14, revised for the context-sensitive rule).**
  - A **global** Randomize All (pressed on a parameter page) creates no level-2 depth parameter and stays within the 915 ceiling (793 with external audio disconnected); repeated presses do not grow the allocated depth-parameter count beyond that ceiling.
  - A Randomize All pressed on a **level-1 grid** **does** materialize exactly that parameter's 225 level-2 depths — and no others (assert no other parameter's L2 depths get materialized as a side effect).
  - **Crunchy is never randomized** by either button, in any view.
  - **Crispy is randomized** by Randomize All on a parameter page (and by Randomize Page on a parameter page).
  - Randomize Page on a **parameter page** changes no depths.
  - Randomize Page on a mod-detail grid (level 1 or level 2) changes only that parameter's own depths.
- [x] 6.12 **Default patch (design D16).** Requires slate indices 6-8 (this section) to exist. On first start, set: VCO1 Shape to minimum, VCO2 Shape to 0.5, VCO3 Shape to maximum; six cross-VCO modulation depth assignments at exactly one encoder-depth-detent — VCO1 pitch ← +1 detent from VCO2 audio and from VCO3 audio; VCO2 pitch ← −1 detent from VCO1 audio and from VCO3 audio; VCO3 pitch ← +1 detent from VCO1 audio and from VCO2 audio (sources are the VCO audio-rate slate entries at indices 6-8, targets are the Audio bank's pitch parameters at slots 0-2); Drive (Drive bank, slot 0) to 20% of its range. Tests: the three Shape defaults read minimum/0.5/maximum exactly; the six depth assignments have the correct sign and source for each of the three VCOs; Drive reads 20% of range; and no parameter outside this enumerated set departs from its ordinary default (assert by diffing the full parameter set against an unmodified baseline).

## 6a. Audio routing: parameters → DSP → stereo output (design D15)

Placed after the modulation slate (section 6) and before the surface layout (section 10) — routing depends on parameters, fuego and modulation, not on the UI. Sections 7-9 are unrenumbered; this section uses a letter-suffixed number so no existing task or section shifts.

- [x] 6a.1 Wire `FroggersApp::ProcessBlock` to be the real audio path (design D15): after `FroggersParameterModel::ProcessSample` runs the fuego seam (packet 5) and the modulation slate (this section) resolves each parameter's post-modulation value, route those values into the ported DSP units — packet 3's original scope plus the Reverb/Drive/Delay additions (tasks 3.8-3.10) — instead of the placeholder finite-silence loop from task 2.1. **Done:** `FroggersApp::RouteAudioSample()` (`app/Froggers.hpp`), called from `ProcessBlock()` immediately after each sample's `parameters_.ProcessSample()`. Reads every knob via `Parameter::CachedKnobValue(0)`, which is verifiably post-modulation (`Parameter::GetRaw()`, `ParameterModulation.cpp:1207-1215`, sums the scene center with `Modulators::ApplyActive()` — modulation-depth routing) and post-fuego (`ApplyFuegoSeam()` runs between `ProcessSamplePhase1`, which bakes `GetRaw()` into the cache at `:1459-1461`, and `ProcessSamplePhase2()`, which only slews `uiDisplayCenters_` and never rewrites the cache, `:1471-1479`) — see `RouteAudioSample()`'s own header comment for the full citation chain. Ordering was achievable as structured; no stop-and-report was needed. Chain: Audio+Envelope banks → 3x `dsp::Vco` + `dsp::MixOscVoices` → Drive bank → `dsp::FrogBlock` + `dsp::DriveBlendPhase` → Filter bank → `dsp::FilterFxChain` → Delay bank → `dsp::StereoDelay` → Reverb bank → `dsp::Reverb`. New, separate DSP-state members added to `FroggersApp` (distinct from `FroggersModulationSlate`'s own private VCOs, which only ever fed the six VCO-audio-rate modulation sources); sample-rate-dependent members (re)initialized in `PrepareToPlay()`.
- [x] 6a.2 Sum the DSP chain's output to the stereo output bus (`block.outputs`), replacing the silent write. **Done:** the mono `RouteAudioSample()` result is written to every output channel each frame in `ProcessBlock()` (D4's `kNumVoices == 1` model — the whole DSP chain, including `dsp::Reverb`/`dsp::StereoDelay`, collapses to a single mono float per sample by construction, matching `FroggersEngine::ProcessSample`'s own `float`-in/`float`-out shape at `FroggersEngine.hpp:850-874`), duplicated identically on both channels rather than computing an independent stereo image.
- [x] 6a.3 FX chain order follows `ApplyOutputFx` (`src/core/FroggersEngine.hpp:822-848`): the parallel comb/peak-blend path (`:824-833`) or the serial delay→comb→bump path (`:834-839`), extended to place the newly ported Reverb, Drive and Delay units in the same relative order the frozen engine applies them. **Done:** Drive occupies `FroggersEngine.hpp:872`'s position (`m_frogBlock.Process(chainIn)`, called before `ApplyOutputFx`). Filter uses the **parallel** branch (`useParallel = true`) — `dsp::FilterFxChain::Process`'s existing parallel/serial split (ported verbatim in packet 3) mirrors `m_useV2FilterParallel`, which the frozen tree sets via `SetUseV2FilterParallel(UsesV2Fuego(hostKind))` (`src/core/DesktopHostIO.hpp:330`, `PagedHostIO.hpp:81`) — the same flag class gating `SetSimIndependentPm` (which task 3.1's Vco port always takes, D7) and `DelayState::setUseV2Layout` (which this port's Delay.hpp always takes, "this app IS the V2 layout"); this app is a V2-fuego host everywhere else that flag appears, so `true` here is the evidenced choice, not a guess. Delay is positioned exactly where the frozen engine's `m_simFxInsert` hook sits (`:840-843`, between the filter chain and Reverb) — confirmed, not inferred, by `sim/WasmSimHost.hpp:34` (`io.m_engine.SetSimFxInsert(simDelayInsertCallback, &delay)`), which wires this port's exact frozen counterpart (`DelayState::processInsert`) into that precise hook; `processInsert`'s own shape (`delay.process(bumpIn, params)` then `delay.toReverbMono(bumpIn, wet, params.dmix)`, `sim/DelayState.hpp:196-198`) is reproduced identically. Reverb is last, matching `:844-847`'s wet/dry blend (folded into `dsp::Reverb::Process`'s own return per its packet-3.8 header comment).
- [x] 6a.4 Output-level safety: no denormals reach the output (flush-to-zero or equivalent), no NaN/Inf under any parameter/modulation combination, and bounded output under self-oscillating comb feedback (`Comb.hpp`'s asymmetric ±1.1 feedback curve, `:66-76`, is a documented self-oscillation case). **Done:** `FroggersApp::SanitizeOutputSample()` (`app/Froggers.hpp`) is the final, unconditional guard on every sample before it reaches `block.outputs` — non-finite → `0.0f`; magnitude below `std::numeric_limits<float>::min()` (a true subnormal/denormal) → flushed to `0.0f`; otherwise clamped to `±8.0f`. This sits on top of, not instead of, the DSP's own internal bounding: the comb's recirculating tap is always re-saturated through `dsp::PadeSaturator` (clamped to `[-1,1]`) before being scaled by a feedback coefficient whose magnitude never exceeds 1.1, and the Reverb's authored Hold control clamps its feedback coefficient strictly below 1.0 — both already analyzed as bounded by construction, confirmed empirically by test `self_oscillating_comb_and_near_unity_reverb_hold_stays_finite_and_bounded` (peak observed 2.94, RMS 1.25, well under the 8.0 clamp — the hard clamp never actually triggered in that run, i.e. the DSP's own bounding held). Also defensively clamped (in the router, not in the frozen/tested packet-3 DSP files): the Filter bank's "Comb offset" seconds value against `dsp::PureDelay::kSize`, since a sufficiently high host sample rate could otherwise push `SetDelaySeconds` past that unit's fixed ring-buffer size.
- [x] 6a.5 Test: for a non-default patch (nonzero Drive, nonzero VCO level, at least one modulation depth active), the app produces **non-silent** finite stereo output over a representative block — explicitly stronger than task 2.3's finiteness check, which silence also satisfies. Assert some output sample exceeds a small epsilon in magnitude. **Done:** `non_default_patch_produces_non_silent_finite_audio` (`app/FroggersAudioRoutingTests.cpp`) — nonzero VCO1 pitch, nonzero Drive (0.8, past D16's own 20%), plus Filter/Reverb/Delay banks pushed off their neutral defaults; explicitly asserts a materialized, active cross-VCO modulation depth (from D16's default patch, task 6.12) rather than assuming one. Observed peak 0.1857, RMS 0.0777 over 2048 captured stereo samples — three orders of magnitude past the `1e-4` epsilon. A second test (`self_oscillating_comb_and_near_unity_reverb_hold_stays_finite_and_bounded`) covers 6a.4's self-oscillation scenario directly (see above).
- [x] 6a.6 Scope gate. **Done:** `git status --porcelain` shows this packet's edits confined to `app/Froggers.hpp`, `app/FroggersParameters.hpp`, `app/Makefile`, `app/FroggersAudioRoutingTests.cpp` (new), and this file — all within `app/**` + the tasks-file exception the packet brief allows. `External/Sheaf`'s gitlink diff predates this packet (task 1.1's pin bump); nothing here touches it. `check-no-frozen-deps`/`check-no-juce` both pass (`nice make -C app -j2 check-no-frozen-deps check-no-juce`).

**Discovered during implementation, not anticipated by any cited line above (reported per this packet's brief, not silently patched around):** the Envelope bank's Sustain parameters (Attack/Sustain/Release × VCO1-3) inherited packet 4's ordinary `defaultValue = 0.0f` (flagged there as deliberately deferred: "a DSP-wiring concern for the packet that actually consumes these values"). `dsp::VcoAdsrState::apply` treats Sustain as a literal target level, not an ExpMap-floored knob like every other bank's parameters, so at the ordinary default the ASR holds at exactly zero regardless of gate state — silencing every VCO. No packet in this change (2 through 10) wires a note-on/gate gesture, so `FroggersApp::PrepareToPlay()` now gates `audioAdsr_` permanently on (see its own comment), and the three Sustain parameters (`FroggersParameters.hpp`'s `FroggersParamSpec::defaultValue`, a new field) default to `1.0f` instead of `0.0f` — resolving packet 4's own deferred decision, exactly as its comment anticipated, rather than expanding 6a's scope. Attack/Release stay at `0.0f` (their ExpMap floor, `kMinTimeSeconds`, is already a fast-but-audible time). This was required for 6a.5/6a.6's own headline claim ("a freshly started app should make sound with no user input") to be true; without it, `default_patch_produces_non_silent_finite_audio` failed silent. Task 6.12's `default_patch_touches_no_parameter_outside_the_enumerated_set` test still passes (verified by rerunning `froggers_modulation_tests`), since this changes what the "ordinary default" *is*, not `ApplyFroggersDefaultPatch`'s own enumerated set.

**AMENDED (operator, 2026-07-27, design D17), then AMENDED AGAIN (operator, 2026-07-27, D17 revised same day):** the premise quoted above — "a freshly started app should make sound with no user input" — is **superseded, not merely revised**, and superseded a second time in the same direction. D17's first revision wired the ASR gate to MIDI note-on/off and was found impossible in Sheaf before any code existed for it (see the since-rewritten §6b.1 finding). D17's *current* form (design.md, revised 2026-07-27) wires the ASR gate to the **master clock's transport quarter-note pulse** instead — no MIDI, no note concept at all. The correct assertion is now: the app produces **non-silent audio while the transport runs**, and **silence while the transport is stopped**, including immediately after `Init()`/`PrepareToPlay()` with the transport never started. `default_patch_produces_non_silent_finite_audio` and `non_default_patch_produces_non_silent_finite_audio` (`app/FroggersAudioRoutingTests.cpp`) have been corrected to start the transport before asserting signal, and a companion test asserting silence with the transport stopped has been added. **This is no longer blocked — implemented** (see revised §6b below). The permanently-on gate in `FroggersApp::PrepareToPlay()` has been replaced with the clock-driven gate computed per-sample in `ProcessBlock()`. The Sustain-parameter default (`1.0f`) is **kept** — full-level sustain remains the correct default for a gated ASR now that a real gate exists.

## 6b. The master clock's transport quarter-note pulse drives the ASR gate (design D17, revised 2026-07-27)

Placed after audio routing (section 6a) and before VCO scopes (section 7) — a clock-driven gate is a routing concern like 6a, not a UI concern (packet 10's scope). Letter-suffixed number per §0's discipline; no existing task or section is renumbered. **This section replaces its own previous content in place** (MIDI note-on/off drive was found impossible in Sheaf before any code existed for it — see 6b.1's finding, retained below for the record — and the operator redirected the same day to a clock-driven gate, which needs no upstream Sheaf change and is implemented here).

- [x] 6b.1 **Verified-absent finding, retained for the record (no longer a blocker — superseded by the clock-driven choice, not resolved by it).** Sheaf ships no MIDI note-delivery mechanism to any app: `include/synth/AppContext.hpp`'s `midiBus`/`instrument` fields carry no raw-note/pitch/gate field; `include/synth/AppConcepts.hpp`'s only optional application hooks are `HasPrepareToPlay`/`HasProcessFrame` — no MIDI hook; `include/synth/ParameterModulation.hpp:930-949` (`MessageIn::Type`) enumerates no note/pitch/gate member; every note-consuming `MidiInProcessor` in `src/MidiController.cpp` (`EncoderMidiInProcessor::Process:725-731`, `SystemButtonMidiInProcessor::Process:881-903`, `PolyphonicPressureMidiInProcessor::Process:851-871`, rejected otherwise at `:823-824`) maps a note onto a pre-configured control-surface target, never delivering pitch+gate to the app; `apps/braid-4/Braid4Core.hpp:196` and `apps/miniapp/MiniAppCore.hpp:256` only install a control-surface MIDI profile. **This is why D17 no longer uses MIDI notes at all** — see design.md D17's "Verified absence" for the full trace. Nothing here is blocked; this task is kept checked because the finding itself is complete and correct, not because MIDI note support was built.
- [x] 6b.2 **Read the transport quarter-note phase and derive the gate duty cycle, following the miniapp idiom exactly (design D17).** Per sample: null-check `block.clockPlan` (`include/synth/AppContext.hpp:84`) and guard on `block.clockPlan->TransportState() == synth::ClockTransportState::Running`; call the **containment-safe** `TryTransportQuarterNotesAt` (`include/synth/MasterClock.hpp:200`, returning `std::optional<double>`) rather than the precondition-carrying `TransportQuarterNotesAt` (`:198`, precondition `Contains(...)`, `:192-198`) — null-checking `clockPlan` alone is necessary but not sufficient for containment. On a present value, `phase = transportQuarterNotes - floor(transportQuarterNotes)`; `gateOpen = phase >= 0.0 && phase < 0.5`. This is `apps/miniapp/MiniAppCore.hpp`'s own gate idiom (guard `:323-324`, phase `:325-327`, duty-cycle assignment `:328` — **note:** an earlier reading of this citation as "`:323-327`" put the assignment inside that range; it is actually the line immediately after, `:328`), reused verbatim in shape, substituting the Try-variant accessor. Implemented in `FroggersApp::ProcessBlock` via a small `TransportQuarterNotesAt(block, absoluteOutputSample)` helper (`app/Froggers.hpp`) called once per frame, feeding `audioAdsr_.setGate(gateOpen)` before `RouteAudioSample()` reads the ASR. The permanently-on `audioAdsr_.setGate(true)` previously in `PrepareToPlay()` (§6a's "Discovered during implementation" note) is removed; `PrepareToPlay()` now only initializes the ADSR's sample rate.
- [x] 6b.3 **Structure the clock read so packet 8 (Marbles/Random S&H clocking, design D8/D8a) does not have to duplicate it.** The gate's clock read is factored into one private helper, `FroggersApp::TransportQuarterNotesAt(block, absoluteOutputSample)`, returning `std::optional<double>` (empty when the transport isn't running or the plan doesn't contain the sample) — the single call site for `TryTransportQuarterNotesAt` in this class. Packet 8 is expected to call the same helper (or a sibling built the same way) rather than re-deriving the null-check/guard/Try-call sequence a second time. Pitch: the previous transposition-law task is **dropped, not implemented and not deferred** — D17 revised removes the transposition source entirely; there is nothing to code here. No new task number is introduced for its absence, per this packet's own instruction not to renumber.
- [x] 6b.4 **Tests.** Covered via §6a's updated audio-routing tests (`app/FroggersAudioRoutingTests.cpp`): non-silent audio while the transport runs (both the default-patch and non-default-patch cases call `rig.StartAt(...)` before asserting signal) and silence while the transport is stopped (transport never started). **Done (packet 7/8/9, 2026-07-27):** the two previously-unwritten tests are now present in the same file — `gate_period_tracks_tempo_change` (drives `rig.Engine().Clock().SetTempoBpm(...)` at two tempos 2x apart and counts gate-open rising edges in captured output, asserting the count scales with tempo) and `missing_clock_plan_does_not_fault_and_leaves_gate_closed` (hand-builds a `synth::AudioBlock` with `clockPlan == nullptr`, poisons the output buffers with a nonzero value, calls `FroggersApp::ProcessBlock` directly, and asserts the poison is overwritten with true zero). Both green.

## 6c. Real-`sheaf-patch`-launcher silence bug: transport does not survive an audio-device reprepare (found post-hoc, 2026-07-27)

Bug report: Frogg3rs launched in the real `sheaf-patch` launcher produced zero audio — no VCOs, no LFOs, randomizing changed nothing — despite all 119 app-level unit tests passing. Every prior audio-producing test (§6a/6b's own `FroggersAudioRoutingTests.cpp`, and this file's own `FroggersSurfaceTests.cpp`) drives `synth_rig::SynthRig<FroggersApp>` and starts the transport exactly once (`rig.StartAt(...)` or one `Play` dispatch); none ever called `synth::Engine::Prepare()` a second time after starting the transport, which is exactly what the real `synth_runtime::Runtime<App>` does on every audio-device renegotiation.

- [x] 6c.1 **Root cause, isolated by direct code trace + headless reproduction (neither of the two leading hypotheses — "transport never starts" / "`ProcessFrame()` never invoked" — panned out; both were verified clean by tracing and by the pre-existing `play_and_stop_controls_exist_and_gate_the_transport` test, which already dispatches `Play` through the real `FroggersUiSurface` and already passed).** `synth::Engine<App>::Prepare()` calls `masterClock_.Prepare(sampleRate, blockSize)` unconditionally (`External/Sheaf/projects/synth/include/synth/Engine.hpp:280-302`), and `MasterClock::Prepare()` unconditionally resets `transportState_` to `Stopped` (`External/Sheaf/projects/synth/src/MasterClock.cpp:929`) with no regard for whether the transport was already `Running`. `Engine::Prepare()` is not a one-time startup call in the real app: `synth_runtime::Runtime<App>::audioDeviceAboutToStart` (`External/Sheaf/projects/synth/runtime/Runtime.hpp:502-511`) re-invokes it on every audio-device renegotiation. Confirmed against a real session's own log (`~/Library/Sheaf/synth/sheaf-patch/logs/2026-07-27T19-37-22-570.log`): three separate "Audio prepared" lines fired automatically within the first ~26 seconds of startup alone (one renegotiating block size 256→512 frames), plus three more when the user manually switched output devices (a natural troubleshooting step for "no audio"). Every one of those silently re-closes the D17 transport-gated ASR gate, with no visual transport-state indicator anywhere in `FroggersUiSurface` and no automatic recovery. Reproduced headlessly (not just read): `synth::Engine<FroggersApp>::Prepare()` called a second time, at the exact sample rate/block size the real device negotiated (44100 Hz / 512 frames), after a `Play` dispatch through the real `FroggersUiSurface`, drops `TransportState()` back to `Stopped` and output back to silence with no further user action.
- [x] 6c.2 **Fix, entirely within `app/` (no `External/Sheaf` edit — the origin of the reset is Sheaf-owned/frozen, but the recovery is implemented at this app's own `PrepareToPlay()` hook, which Sheaf's `Engine` already calls after every `Prepare()`, including every reprepare).** `FroggersAppCore` gains `desiredTransportRunning_` (an atomic bool, default `false`, preserving every existing test's "silent until Play" default), set by `FroggersUiSurface::HandleAction`'s `kPlay`/`kStop` branches alongside their existing `uiBus` push. `FroggersAppCore::PrepareToPlay()` now re-pushes the same `MessageIn::Start` the Play button itself pushes (onto `context_->uiBus`, the same message-thread-producer/audio-thread-consumer path already sanctioned for transport Start/Stop) whenever `desiredTransportRunning_` is true — so the very next `ProcessBlock` (which always drains `uiBus` before touching the clock plan) restores `Running` before another sample is produced, on every reprepare, not just the first.
- [x] 6c.3 **Regression test exercising the real path, not `rig.StartAt`.** `app/FroggersSurfaceTests.cpp`'s new `transport_survives_audio_device_reprepare_after_play`: constructs `SynthRig<FroggersApp>` at the real device's negotiated settings (44100 Hz / 512 frames), dispatches `Play` through the real `FroggersUiSurface::DispatchAction` (not `rig.StartAt`), asserts non-silent output, then calls `synth::Engine::Prepare()` a second time (exactly `audioDeviceAboutToStart`'s own call) and asserts audio survives it and `TransportState()` is still `Running`. Also asserts `Randomize All` still audibly changes the output after the reprepare (closing the "randomizing changes nothing" half of the report), and that an explicit `Stop`'s intent likewise survives a reprepare (the fix must not make the transport unstoppable). Verified this test fails at exactly the post-reprepare assertion without 6c.2's fix, and passes with it. Full suite green (119 pre-existing tests + this one).
- [ ] 6c.4 **Latent, not fixed here (flagged per this packet's own instruction not to silently patch around discoveries):** a persisted `config.json` with `sync.receiveClock=true` from a prior external-MIDI-clock session would make `MasterClock::ApplyPendingInternalTransport`'s `activatesNow = startCommand && !syncConfig_.receiveClock` (`MasterClock.cpp:845`) arm rather than run the transport on internal `Play`, silently reproducing the same "Play does nothing" symptom for a different reason. Not the cause of this specific report (the session's own `config.json` had `receiveClock:false`), but worth its own defensive UI affordance (the BPM slider already renders read-only while externally clocked — task 10.6 — but Play/Stop do not).

## 7. VCO scopes

- [x] 7.1 VCO exposes `UIState{connected, scope, scopeChannel, scopeColor}` (`include/synth/DspOscillators.hpp:119-124`) + `SetScopeWriterHolder()` (`:133-135`), `SetScopeColor()` (`:137-139`), `PopulateUIState()` (`:165-171`). (`:141-163` is `Process` — the earlier `:141-171` citation was wrong.) **Done:** `dsp::Vco` (`app/dsp/Vco.hpp`) gains this UIState shape verbatim, embedded directly in the same struct as its `Process()`, mirroring `WavetableVco`'s own placement exactly rather than a separate wrapper. Every cited line matched the brief's claim exactly (verified by reading `DspOscillators.hpp` with line numbers before porting).
- [x] 7.2 One `ScopeWriter` (`include/synth/DspScope.hpp:76`); `ReserveChans` per VCO (`:89`) returning a `ScopeWriterHolder` (`:15`); `PopulateUIState` each block. **Done:** `FroggersApp` owns one `synth::ScopeWriter` + three `ScopeWriterHolder`s (one `ReserveChans(1)` per VCO, in its own constructor), and calls each VCO's `PopulateUIState()` plus `vcoScopeWriter_.Publish()` once per block, at the end of `ProcessBlock()`.
- [x] 7.3 Replace Braid's two scope grids with Froggers VCO scope panels via `ScopeVisualizer<UIState>` (`include/synth/PortableUIBuilders.hpp:225-226`, which reads `connected/scopeColor/scope/scopeChannel` at `:253-256`). **Done, with a scope caveat flagged per the operator brief's hard constraint ("no surface layout work — that is packet 10"):** `FroggersApp` constructs one `synth::ui::ScopeVisualizer<dsp::Vco::UIState>` over its three VCOs' UIStates and exposes it via `VcoScopeVisualizer()` — the visualizer object itself is real and renders correctly (proven by task 7.4's test), but it is deliberately NOT inserted into `FroggersUiSurface::BuildTree()`'s node tree, since placing panels in the surface is packet 10's job, not this one's. "Replace Braid's two scope grids" (an actual panel-placement action) is therefore the one piece of this task's literal wording not done here — recorded as a deliberate scope boundary, not an oversight.
- [x] 7.4 Tests: scope channels bound; ~~panels present~~ (dropped — no panels are placed by this packet, see 7.3's note); visualizer connected flag true after processing. **Done:** `app/FroggersVisualizerTests.cpp`, `vco_scope_channels_bound_and_visualizer_connected_after_processing` — confirms `connected == false` before any `ProcessBlock()`, then `connected == true` + correct `scopeChannel` (0/1/2) + non-null `scope` after running blocks, and that `Draw()` produces at least one command.

## 8. Marbles: clock + visualizer

- [x] 8.1 **Advance Marbles on transport quarter notes.**
  - Use **`TryTransportQuarterNotesAt`** (`include/synth/MasterClock.hpp:200`), returning `std::optional<double>`, guarded on `has_value()` — Braid's idiom (`apps/braid-4/Braid4Core.hpp:500-503`). The unchecked `TransportQuarterNotesAt` (`:198`) carries a documented **precondition `Contains(absoluteOutputSample)`** (`:192-198`); null-checking `block.clockPlan` (`include/synth/AppContext.hpp:84`, set at `include/synth/Engine.hpp:402`) is necessary but NOT sufficient. **Done, and NOT re-derived:** `FroggersApp::ProcessBlock` computes `transportQuarterNotes` once per sample (task 6b.3's own `TransportQuarterNotesAt()` helper) and passes that SAME value into `FroggersModulationSlate::Step()` — packet 8 never calls `TryTransportQuarterNotesAt` a second time.
  - Per-source rate mechanism, since the six sources do NOT all advance once per quarter note: `Phasor2Tick::Input` is `{double time, int multiplier}` (`include/synth/DspPhasor2Tick.hpp:12-15`) and `IsValid` rejects `multiplier <= 0` (`:17-22`). So rate **multiplications** use `multiplier` (#1 and #4 = 1, #2 = 2, #3 = 3) and rate **divisions** must pre-scale `time` (#5 = QN/4 with `multiplier = 1`). See design D8a. **Done:** `FroggersModulationSlate::StepClockDrivenLanes()`, one `synth::Phasor2Tick` per lane, exactly this mapping.
  - **Source #6 is not tick-driven at all.** It is a `GangedRandomLfo` (`include/synth/DspRandomLfo.hpp:311-325`) whose `RandomTimingConfig::muSeconds` (`:18-22`) is recomputed each block from `clockPlan->QuarterNotesPerSample()` (`include/synth/MasterClock.hpp:179`) so one move spans 16 quarter notes. Clamp the derived seconds finite and positive before it reaches the audio thread — `Process` calls `ValidateInput` **every sample** and `ValidateRandomTimingConfig` **throws** (`:26-30`). **Done:** `FroggersModulationSlate::PrepareBlockClock()`, called once per block from `FroggersApp::ProcessBlock` before the per-sample loop; falls back to a 120-BPM-equivalent quarter-note duration when no clock plan is available, so the config is always finite/positive.
- [ ] 8.2 External MIDI clock sync via `SyncConfig`/`HandleExternalClock` (`include/synth/MasterClock.hpp:15,344-350`); verify tempo changes track. **Left unchecked, honestly scoped down:** no app-side code is needed for sync itself — the app only ever reads `MasterClock`'s already-synced `QuarterNotesPerSample()`/`TryTransportQuarterNotesAt()` output regardless of clock source, so external-clock sync works "for free" through the exact same code path task 8.1 wires. What was actually verified (`FroggersAudioRoutingTests.cpp`'s `gate_period_tracks_tempo_change`) is tempo-tracking via the **internal** `MasterClock::SetTempoBpm` path, not a genuine external MIDI clock byte stream driving `HandleExternalClock`/PLL convergence — that would need `SyncConfig{.receiveClock=true}` plus a realistic timed sequence of `ExternalClockAt(...)` pulses and was judged disproportionate effort for this packet; flagged here rather than silently marked done.
- [x] 8.3 Visualizers, both attached to the **modulator** via `ModulatorMetadata::visualizer` (`ParameterModulation.hpp:251`) — this is what `ModulationDepthConfig` copies into each depth cell (`src/ParameterModulation.cpp:1872`), **not** `ParameterConfig::visualizer`:
  - X-style sources (1–5): a **new** `Visualizer` rendering the remembered loop as a waveform with a playhead at the current index. **Done:** `synth_froggers::RandomShLaneVisualizer` (`app/FroggersRandomShVisualizer.hpp`), reading a new `dsp::RandomShLane::UiState` (`app/dsp/RandomShLane.hpp`) populated once per block.
  - Y-style source (6): reuse `GangedRandomLfoVisualizer` directly (`include/synth/GangedRandomLfoVisualizer.hpp:247-248`) — a smooth random walk is exactly what it draws. **Unchanged from packet 6** (`source6Visualizer_`); confirmed by type (`dynamic_cast`) in `FroggersMarblesClockTests.cpp`.
- [x] 8.4 Tests: over N quarter notes each source advances exactly N × its own rate multiple (#1, #4 → N; #2 → 2N; #3 → 3N; #5 → N/4), and source #6 is verified as tempo-proportional rather than counted in ticks; a high deja-vu source cycles a fixed loop while a low one keeps producing new values; the Y-style source is unaffected by deja-vu and steps at its slower rate; visualizer state matches the bag. **Done**, all in `app/FroggersMarblesClockTests.cpp` unless noted: `per_source_rate_ratios_match_design_d8a_over_eight_quarter_notes` (exact counts 8/16/24/8/2 over N=8 QN); `source_six_tempo_following_input_scales_inversely_with_quarter_notes_per_sample` (formula-level: doubling `quarterNotesPerSample` exactly halves the derived mu-seconds — "tempo-proportional," verified directly rather than by waiting out real LFO rounds); `locked_loop_source_cycles_a_fixed_loop_across_two_full_cycles` / `free_running_source_...` (dsp-level, in `FroggersDspParityTests.cpp`); `source_six_visualizer_is_the_ganged_random_lfo_kind_others_are_not` (source #6 is categorically not a `RandomShLane` at all — no `dejaVuKnob` concept exists for it, a structural rather than behavioral proof of "unaffected by deja-vu"); `visualizer_state_matches_the_bag_after_publishing`.
- [x] 8.5 Confirm no source-level control exists: no Random S&H behaviour parameter in any bank, no seventh bank (D8). Depth encoders are unaffected — assert they are bipolar and default to neutral/off. **Done:** `no_random_sh_behaviour_parameter_exists_in_any_bank_and_no_seventh_bank` (asserts `kFroggersBankCount == 6` and scans every bank's parameter names for "Step chance"/"Deja vu"/"Bag size"/"Slew"/"Spread"/"Bias") and `random_sh_depth_encoders_are_bipolar_and_default_neutral` (`Parameter::Range() == RangeKind::Bipolar`, `SceneCenter(0) == 0.5` i.e. `kNeutralModulationDepthCenter`), both in `app/FroggersMarblesClockTests.cpp`.
- [ ] 8.6 **Validate D8a's character table by ear** before it is treated as settled. Confirm the three locked loops read as 2-bar / 1-bar / polyrhythmic-triplet, that #3's 8-step loop resolves 3-against-2 every two bars, and that the six are usefully distinct. **#4's ~5-level quantisation is the flagged weak choice** — expect to revise it. Changing this table after patches exist shifts every patch, so settle it here. **Clock convention:** `MasterClock` supplies no bar or time signature, so this change defines **1 bar := 4 quarter notes (4/4)**, and "realigns with the barline" means realigns with the 4-QN grid. **Left unticked — cannot be done by a subagent** (requires listening to the running app), per this packet's own operating brief.

## 9. Bump/Comb transfer-function visualizers

- [x] 9.1 Give `ResonantBump` and `Comb` a `struct UIState : TransferFunction` implementing `FrequencyResponse(float)`/`TransferFunctionValue(float)` (`include/synth/DspTransferFunction.hpp:7,9,10` — the type is `TransferFunction`; `DspTransferFunction` is only the file name; shape per `include/synth/DspFilters.hpp:22,85,143`). **Done, all citations confirmed exact:** `app/dsp/FilterFx.hpp`'s `ResonantBump::UIState`/`Comb::UIState`, mirroring `OnePoleLowPass::UIState`'s in-struct placement (`DspFilters.hpp:22`). The comb's closed form linearises its Padé saturator as an identity (small-signal gain exactly 1.0 at x=0), per D10's explicit instruction. This deliberately widens `app/dsp/*.hpp`'s established "no Sheaf dependency" convention by one small, header-only, link-free include (`synth/DspTransferFunction.hpp`) — flagged in the packet report; `app/Makefile`'s `DSP_TEST_BIN` rule updated accordingly (same widening as task 7.1's VCO scope UIState).
- [x] 9.2 One `Visualizer` sampling the response into `DrawCommand::Polyline` (`include/synth/PortableUI.hpp:101,105`) — log-frequency axis; handles the comb's periodic response and the bump's peak/notch. **Done:** `synth_froggers::TransferFunctionVisualizer` (`app/FroggersTransferFunctionVisualizer.hpp`), generic over any `synth::TransferFunction`, sampling log-spaced frequencies into dB, clamped to a fixed `[-40,40]` dB window before mapping to pixel-y (see 9.4).
- [x] 9.3 Attach to the relevant filter parameters as underlays. **Done:** `FroggersParameterModel::Init()` takes optional `peakVisualizer`/`combVisualizer` pointers (owned by `FroggersApp`, constructed ahead of `Init()`) and sets `ParameterConfig::visualizer` (D9's per-parameter underlay mechanism, distinct from task 8.3's `ModulatorMetadata::visualizer`) on the Filter bank's "Peak freq/Peak gain/Peak Q" (paramIx 1-3) and "Comb delay/Comb feedback/Comb LP" (paramIx 4-6) rows. "Comb offset" (a timing control ahead of the comb, not part of its own curve), "Comb/Peak" (a blend) and "Scoop" (a second `ResonantBump` instance, out of this task's two-filter scope) deliberately get no underlay — an implementer judgement call, flagged here rather than silently decided.
- [x] 9.4 Tests: response curve matches closed-form at sample points; self-oscillating comb feedback produces only finite plot values **and the displayed magnitude is bounded rather than drawn out of frame**. **Done**, split across two files: `FroggersDspParityTests.cpp` (`resonant_bump_frequency_response_matches_simulated_steady_state_gain` — an independent simulation cross-check, not merely self-consistency; `comb_zero_feedback_response_is_exactly_unity`; `self_oscillating_comb_response_is_finite_and_bounded`, feedback at `Comb::GetFeedback`'s +1.1 ceiling, densely sampled) and `FroggersVisualizerTests.cpp` (`self_oscillating_comb_visualizer_plot_stays_within_bounds` — same worst-case feedback, asserts every plotted point's y stays within the panel's own bounds, not merely finite; `silent_comb_visualizer_plots_flat_unity_response` as a control).

## 10. Surface layout (ported v2 design)

- [x] 10.1 Portable surface via `synth::ui::Builder` following Braid's composition (`apps/braid-4/Braid4UI.hpp:31-96`; layout math `apps/braid-4/Braid4UiModel.hpp:70-88,90-206`). **Done:** `app/FroggersUiSurface.hpp`'s `FroggersUiSurface`, composed onto `app/FroggersAppCore.hpp`'s `FroggersAppCore` via `app/Froggers.hpp`'s thin `FroggersApp : public FroggersAppCore` (mirrors Braid4Core/Braid4UI/Braid4's own split exactly).
- [x] 10.2 Top band: VCO scope panel beside transport + global chrome (**Randomize All** only, scenes — see 6.8). **Transport means explicit `Play` and `Stop` controls, carried over from v1/v2** (`desktop/Source/MainComponent.h:50-51`; `desktop-v2/Source/MainComponent.h:54-55`) — operator instruction 2026-07-27, not dropped. Since design D17 gates the ASR from the transport quarter-note pulse, these are now the instrument's master start/stop: with the transport stopped the app is silent (verified — peak 0.000), and Play starts it. Ported the affordance (push `MessageIn::Start`/`Stop`), not the JUCE implementation (D11). **Play/Stop are coloured-icon Draw nodes, not text Buttons (operator 2026-07-27, Change 3)** — green triangle / red square on white, explicit computed `Bounds`, label kept for accessibility; see design.md D11. **Randomize Page is not in the chrome band** — it lives in the per-page/bank header (10.7); the chrome band carries exactly one randomize control. Chrome band includes the BPM slider beside the scene slider (10.6). **Crunchy dropped from the chrome list (operator 2026-07-27, superseding this task's earlier chrome-band Crunchy control): "why is there a fucking slider for crunchy between the randomize buttons, i never asked for that. It duplicates bank slot 15."** The chrome band no longer includes Crunchy at all; Crunchy exists only at bank slot 15 (design.md D11/Resolved-decision 22) — accepted trade-off: unreachable while a modulation view is open, since slot 15 is then Target/Back. **Discrepancy from this task's own wording, flagged not silently adapted:** "dual VCO scope panels" does not match what packets 7-9 built — `FroggersAppCore::VcoScopeVisualizer()` is exactly ONE combined `ScopeVisualizer<dsp::Vco::UIState>` with three overlaid layers (VCO1/2/3), not two separate panels; one panel is placed in the scope area.
- [x] 10.3 Bank selector (direct-select, no arrow paging) + 16-slot encoder grid, slots 0–15 with Crispy at 14 and Crunchy at 15.
- [x] 10.4 Drill-in swaps the grid **in place** to the mod-detail grid; single active-bank authority (no parallel page state). Encoder press is routed through `FroggersAppCore::RequestEncoderPress` -> `ProcessFrame()` -> `FroggersModulationDrillIn::PressEncoder` (packet 6), never through a generic `MessageIn::ParamPush`, so the app's own 2-level drill-in cap is never bypassed by the surface.
- [x] 10.5 Tests (`app/FroggersSurfaceTests.cpp`, 9 tests): layout bounds; in-place swap; no overlap at the target window size; every encoder cell fully inside the grid region; **the encoder ring renders the fuegoized value with no visualizer-specific UI code** (the rendered-surface half of 5.5) — verified both structurally (`EncoderDrawState`/`EncoderVoiceDrawState`, `EncoderDraw.hpp:279-304`, have no `rawKnobValue` field at all) and at runtime (`encoder_ring_renders_fuegoized_value_not_raw_scene_center`).
- [x] 10.6 **BPM slider in the chrome band (operator, 2026-07-26).** Placed immediately beside the scene slider. Writes `MasterClock::SetTempoBpm(double)` (`include/synth/MasterClock.hpp:318`) and displays `TempoBpm()` (`:321`), both called only from `FroggersAppCore::ProcessFrame()`/`ProcessBlock()` (audio thread, per `AppContext.hpp`'s thread-role contract) via a pending-atomic bridge from the UI thread — never called directly from `DispatchAction`. **Read-only/inert while slaved:** `SetTempoBpm` returns false and no-ops when `syncConfig_.receiveClock` is set (`src/MasterClock.cpp:963-965`, confirmed exact); `TempoBpm()` then reports the recovered external tempo (`activeBpm_ = recoveredBpm;` at `src/MasterClock.cpp:1182`, confirmed exact). The surface renders a non-interactive `StatusText` (not a `Slider`) while slaved — Builder's `Slider`/`Button`/etc. node kinds have no "disabled" parameter to express read-only any other way. Both states tested.
- [x] 10.7 **Randomize controls placement (D14, operator 2026-07-26).** Randomize All takes the chrome-band position; Randomize Page is in the per-page/bank header, restoring the position removed at `desktop-v2/Source/ui/SubmodulePagePanel.cpp:11-13`. **"Rand waveforms" and "Rand Resample" are not ported** (retired per design D14) — confirmed absent by test. Ported the positions, not the JUCE implementation (D11).

## 11. Browser package, website cutover + hosting (six-step publish pipeline, design D12)

**Gate (UPDATED 2026-07-27): the repository-rename gate has passed.** The repo is renamed to **`frogg3rs`** (not `froggers3`, as this gate was originally worded), and the local git remote is already updated to `git@github.com:daguilarc/frogg3rs.git`, confirmed reachable (design D12, task 11.5). **The Sheaf-fork blocker that briefly replaced it is ALSO resolved (2026-07-27, late):** the submodule was reverted to upstream pin `1940ddcb`, reachable on `origin/main` of `jvictor0/Sheaf`, so the gitlink resolves from any clone or CI runner. **No publication blocker remains from either cause.** Nothing public is committed, pushed, or released under the old name.

**11.6 onward remain unstarted, but are now gated on product readiness rather than infrastructure** — see the successor change for the GUI defects (dead oscilloscopes, unrecoverable audio blowups) that must land before anything is worth deploying.


- [x] 11.1 **Prerequisite + manifest (pipeline step 1).** Compile Sheaf's browser tooling first — it is TypeScript and ships unbuilt (`browser/dist/src/*.mjs` does not exist until compiled); every command below depends on this. Then author our own build manifest, same shape as `browser/first-party-apps.json`: `schemaVersion`, `publisher{id,name}`, and per app `appId, displayName, author, category, header, cppType, includeDirs` — every field required, **no extras**. Identity (design D12, resolved): `publisher.id` = **`daguilarc`** (**permanent** — record why: patches are addressed at `patches/<publisher-id>/<app-id>`, so changing it later orphans every saved patch), `publisher.name` = `daguilarc`, `appId` = `frogg3rs`, `displayName` = **"Frogg3rs Synth"**, `author` = `daguilarc`, `category` = `synth` (matching Braid 4; note Sheaf's own two first-party apps disagree with each other on category casing). Gotcha: `includeDirs` entries are **relative POSIX paths resolved against `browser/`** (`..` allowed, absolute rejected) — from that root our app directory is five levels up (`../../../../../app`); `header` is a **bare filename** located by searching `includeDirs`, no `..` in it. (The CLI's own `--manifest`/`--allowed-source-root` flags do accept absolute paths; only in-manifest `includeDirs` do not.)
- [x] 11.2 **Compile (pipeline step 2; needs emsdk — `emcc` is not currently installed on this machine).** Browser build linking `BrowserRuntimeAbi.cpp` + `SYNTH_BROWSER_APP(FroggersApp)` (`include/synth/browser/BrowserAppEntry.hpp:18-26`, which asserts `BrowserApplication` = `synth::SynthApplication` at `:20`/`:9`) via `build-browser-apps.mjs` against our manifest and app source root, `--output-root` resolving beneath `browser/dist/wasm`, mirroring the exported symbol surface (`browser/src/build-browser-apps.mjs:24-41` — `EXPORTED_FUNCTIONS` `:24-36`, `EXPORTED_RUNTIME_METHODS` `:38-41`) and the emscripten flags (`commonCompilerArgs` at `:43-65`; `compilerArgs`, the invocation assembler, at `:67-76`) exactly. Copy artifacts out of `browser/dist/wasm` afterwards (see 11.7).
- [x] 11.3 **Package (pipeline step 3 — do NOT reimplement).** Run Sheaf's packager — it does the sha256 inventory, symlink rejection, and writes the immutable `packages/<app-id>/<build-id>/` layout with a content-derived `buildId` via `assemblePackage` (`browser/src/package-contract.mjs:11-17,92-112,120-176` — `assemblePackage` validates roles AND computes the buildId AND writes bytes AND builds the manifest; the pure role validation is `validateArtifactRoles` at `:76-90`, invoked at `:140`), printing `{appId, buildId, browser:{...}}` on stdout. The emission directory must contain **only** files named by one of the 5 artifact roles or it errors; three of the five roles legitimately point at the same entry file. `appId` = `frogg3rs`, publisher id is ours (not `sheaf`); both satisfy the id pattern `/^[a-z0-9]+(?:-[a-z0-9]+)*$/` (`browser/src/app-build-manifest.mjs:8`) — also confirmed to satisfy `IsValidSynthAppId` (`include/synth/AppRegistry.hpp:33-46`).
- [x] 11.4 **Catalog (pipeline step 4).** Emit a schema-v1 `catalog.json` at `catalogs/daguilarc/catalog.json`, **sibling to `packages/`**, splicing in step 3's stdout and wrapping it with `schemaVersion`, `catalogVersion`, `publisher` (`browser/docs/catalog-schema-v1.md:20-45`; the launcher accepts `SUPPORTED_CATALOG_SCHEMA_VERSION = 1`, `browser/src/catalog.ts:12`), validated against Sheaf's parser expectations. `:216-238` is `parseBrowserPackage`, an unexported internal helper called from `parseApp`; only `:258-279` (`export function parseCatalog`) is a callable validation entry point (`browser/src/catalog.ts:216-238,258-279`). `catalogVersion` is just a trimmed 1–128-character string — Sheaf's `first-party-<digest>` prefix is convention, not requirement. App ordering within the catalog does not matter; the launcher sorts by display name at merge.
- [x] 11.5 **Repository renamed to `frogg3rs`** (not `froggers3`, as originally planned) — **complete, 2026-07-27**. Repo/origin renamed; local git remote updated to `git@github.com:daguilarc/frogg3rs.git`, confirmed reachable. `appId` is unaffected (`frogg3rs`); `publisher.id` is unaffected (`daguilarc`, permanent). **Operator-visible product naming in the app header is unchanged** — this was repository/origin/URL only. **Left for task 11.6, not yet done:** the Pages base path (still `VITE_BASE=/FroggersTiga/` in `pages.yml`) must become `/frogg3rs/`, and any hardcoded origin in the publish tooling must be found and updated to match. Concrete URLs for 11.6 to target: site `https://daguilarc.github.io/frogg3rs/`, catalog `https://daguilarc.github.io/frogg3rs/catalogs/daguilarc/catalog.json`.
- [ ] 11.6 **Website cutover (D13).** Rewrite `.github/workflows/pages.yml` to build and deploy **the new app** as the site, with the Sheaf catalog + package (pipeline steps 1–4) published to a subpath of the same site in one atomic job. Remove the `wasm/`+`web/` build steps and the `sim/` preflight scripts that exist to serve them. The `sim/` preflight step (`pages.yml:26-36`) also runs two non-`sim/` checks in the same block: `node scripts/generate-host-display.mjs --check` (`:33`) and `node scripts/verify-host-display-shape.mjs` (`:34`). Removing the workflow step orphans those scripts but does not edit them — `scripts/` is neither frozen nor in §0's permitted-edit list, and their deletion belongs to follow-on A. **Do not edit `wasm/` or `web/`** — they go dormant, unmodified; deletion is follow-on A.
- [ ] 11.7 **Clean or exclude `browser/dist` build artifacts.** `browser/dist` is **not gitignored**, and pipeline steps 1–3 write compiled tooling and wasm output as files inside the Sheaf submodule's own working tree — this collides with the frozen-tree/scope verification below (11.8) unless the build output under `browser/dist` is cleaned or explicitly excluded after artifacts are copied out (11.2/11.3).
- [ ] 11.8 Verify the frozen-tree property still holds after the workflow rewrite and the `browser/dist` cleanup: `git diff` over `wasm/` and `web/` is empty, `git status` over `External/Sheaf` shows no dirty `browser/dist` output, and the only file changed outside the new app tree (plus the permitted gitlink) is `pages.yml`.
- [ ] 11.9 **Host (pipeline step 5).** Serve with CORS + correct media types — `Access-Control-Allow-Origin: *`, `text/javascript` for `.js`, `application/wasm` for `.wasm`; verify no client can observe a catalog referencing partially uploaded artifacts. GitHub Pages satisfies this.
- [ ] 11.10 **Self-certify (pipeline step 6).** Validate the deployed catalog with Sheaf's `browser/src/validate-deployed-catalog.mjs` against the live URL — it checks CORS, MIME, decoded size, and SHA-256 of every declared file, with no Sheaf-specific hardcoding.
- [ ] 11.11 Load-test through the launcher cross-origin (pattern: `browser/tests/two-origin-package.spec.ts:1-56`).
- [ ] 11.12 Site smoke: the published site serves the new app, audio runs, UI is interactive, and no artifact of the old web app is served.
- [ ] 11.13 **Registration submission.** Hand Sheaf, for `catalog-sources.json` (`browser/catalog-sources.json:1-3`): (1) the absolute HTTPS catalog URL, used verbatim; (2) `publisher.id` (`daguilarc`) and name; (3) the `appId`s we plan to publish (dedup is on `<publisher-id>/<app-id>`, so no collision with `miniapp`/`braid-4` given our distinct publisher id); and (4) confirmation that 11.10's self-certification passes live. Their side is a one-line change; no Sheaf source work by us. **Only after 11.5**, so the URL never needs reissuing. Record the trust-boundary consequence (design D12): listing means our wasm executes inside the Sheaf launcher's origin, and hash verification is tamper-resistance in transit, not a sandbox against us.

## 12. Verification + docs closure

- [ ] 12.1 Full app test suite + parity suites green — run via subagent, pass/fail + tail only (OMNI §16.1).
- [ ] 12.2 Desktop smoke through `sheaf-patch`, built with the `EXTRA_APP_*` variables (task 2.2): assert Froggers appears alongside Braid 4 and Miniapp in the launcher, and separately that a build of `sheaf-patch` with the `EXTRA_APP_*` variables **unset** produces an unchanged launcher (no Froggers entry, no `-DSHEAF_PATCH_EXTRA` reaching the compiler). Then: app launches, scopes live, banks switch, drill-in reaches level 2 and refuses level 3, Marbles follow the clock, **Randomize All produces an audibly different patch without touching Crunchy, and Randomize Page inside a mod-detail grid changes only that grid**. Additionally: **Randomize All pressed on an L1 mod-detail grid audibly changes second-layer (L2) behavior** for that parameter's modulation sources, distinct from its parameter-page behavior. And confirm **neither retired button ("Rand waveforms", "Rand Resample") appears anywhere in the surface** — only Randomize All (chrome band) and Randomize Page (per-page header) are present.
- [ ] 12.3 Browser smoke: package loads in the launcher, audio runs, UI interactive.
- [ ] 12.4 **Frozen-tree proof.** `git diff <baseline-from-1.0>` shows zero modifications under `desktop-v2/`, `desktop/`, `src/`, `sim/`, `wasm/`, `vcv/`, `web/`, and no edits outside the new tree beyond §0's permitted list. Firmware parity must compare **`FroggersTiga.bin` only** — comparing out-of-tree build artifact hashes against the baseline's other tracked artifacts guarantees false mismatches: `.elf` embeds 22, `.map` 64 and `.d` 408 occurrences of the absolute build path. Only `FroggersTiga.bin` is build-location independent (0 absolute paths) and is therefore the only valid parity artifact. There is no `__DATE__`/`__TIME__` anywhere under `src/`, **but the "genuinely reproducible" claim that used to follow here is FALSE — audited 2026-07-27.** The tracked `.bin` does not reproduce from baseline sources with the current toolchain: baseline sha256 `6ca56ee8…` / 88964 bytes versus a fresh out-of-tree build `be0dc826…` / 89172 bytes, diverging from byte 5 in the ISR vector table, even though our own build is internally deterministic. **The output-hash half of this task is therefore INVALID and stays open**; do not treat a hash mismatch here as evidence of frozen-tree damage. The source half — zero modifications under the seven frozen trees — passes outright, and that is what actually protects Daisy. `BUILD_DIR ?= build` (`src/mk/config.mk:10`) is the out-of-tree override knob. Build the Daisy firmware to a scratch directory via that knob and compare the `.bin` hash to the baseline's. Do **not** build in place — `src/FroggersTiga/build/` holds 5 tracked artifacts, so an in-place build rewrites frozen-tree files and invalidates the proof it is meant to establish.
- [ ] 12.5 Docs: app README (build, run, package, publish), the DSP-fork note (D3), the fuegoize-UB divergence note (D6), the pin policy (D1), and the resolved decisions.
- [ ] 12.6 **QA judgement calls (D14) — settle by ear.** Whether ~61 L1 depth changes per Randomize All press (parameter-page case) is the right aggregate reach, and whether the 50% no-op rate on a single Randomize Page press in a mod-detail grid is acceptable. Both are tune-by-ear items in the same class as design D8a's character table. **Neither is fixed by editing Sheaf's constant** — the app-side lever is target-set selection (task 6.8). A third: **judge whether Randomize All on an L1 grid reaching L2 (materializing 240 parameters for one focused parameter) is desirable in use or too destructive** — this is the one randomize path that creates L2 storage rather than re-randomizing what's already there, so it warrants its own by-ear check separate from the first two.

## Out of scope (do not implement in this change)

- Any edit to `desktop-v2/`, v1 `desktop/`, `src/` (incl. Daisy firmware), `sim/`, `wasm/`, `vcv/`, `web/`.
- Fixing `sim/Fuegoize.hpp:23`'s divide-by-zero in the frozen trees — recorded in design D6, deferred to its own change.
- VST/AU hosting — **deferred to follow-on C**, not abandoned.
- Deleting `desktop-v2/`, `wasm/` or `web/`, and stripping the `src/core/` V2 gates — **deferred to follow-ons A and B**. This change supersedes those trees; it does not remove them.
- MIDI mapping/profile redesign; polyphony.
- Retiring the Daisy product.
- Byte-identical audio parity with desktop-v2 (explicitly disclaimed — design D6).
