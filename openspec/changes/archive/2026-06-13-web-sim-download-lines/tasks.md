## 1. Markup

- [x] 1.1 Add `.sim-meta` block in `web/index.html` after `</header>`, before `.controls-top`, with download and legal lines and GitHub links

## 2. Style

- [x] 2.1 Add `.sim-meta` rules in `web/src/style.css` — compact typography, link color, spacing
- [x] 2.2 Reduce `.subtitle` bottom margin so total header-to-transport gap stays balanced

## 3. Verify

- [x] 3.1 Visual check: lines sit between subtitle and Play/Stop; column alignment unchanged at desktop and mobile widths
- [x] 3.2 Link targets: macOS DMG, Windows Setup, MIT License URLs resolve (after release tag published)

## 4. Archive

- [x] 4.1 Archive change; merge spec into `openspec/specs/web-sim-download-lines/`
