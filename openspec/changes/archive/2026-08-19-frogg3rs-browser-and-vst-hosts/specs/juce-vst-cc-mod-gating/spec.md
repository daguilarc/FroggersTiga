# Delta — `juce-vst-cc-mod-gating`

**Removed 2026-08-18 in `frogg3rs-browser-and-vst-hosts`, in its
entirety.** The capability specifies the deleted `desktop/` v1 VST
wrapper (never tested at the AudioProcessor layer, never built by CI;
wrapper deleted in commit `0be9ab0`). The spec directory is deleted at
archive-time sync. This delta file was added at the second 2026-08-18
omni-rule audit: the removal was previously recorded only as prose in
the `froggers-vst-host` delta, but repo convention (archive precedent,
sync-skill delta format) is one delta file per affected capability.

## REMOVED Requirements

### Requirement: Plugin-hosted CC enable controls

### Requirement: DAW CC respects enable flags

### Requirement: Toolbar MIDI button functional

### Requirement: vst-v2-midi-gating-policy
