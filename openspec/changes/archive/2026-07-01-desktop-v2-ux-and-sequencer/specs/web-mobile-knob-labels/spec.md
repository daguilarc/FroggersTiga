## MODIFIED Requirements

### Requirement: Mobile Audio labels visible

On mobile viewport (≤430px width), Audio page knob labels SHALL remain readable without horizontal clipping on core and pair-AR cells.

#### Scenario: Pair-AR mobile labels

- **WHEN** mobile Audio page renders
- **THEN** labels include `Attack 1+2` (not `Att. 1+2`) with visible bounding boxes
