{
  "name": "frogg3rs-site-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright layout regression suite for the published frogg3rs site (app/browser/site/, served from app/browser/dist/site). Its OWN harness/package.json, deliberately separate from External/Sheaf's own test suite (Sheaf source is never edited).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
