TARGET := FroggersGuitar
SRCS := FroggersGuitar.cpp

# Selects the no-reverb, dry-external-in-parallel build. Set before the
# include: daisy.mk composes DEFS immediately.
DEFS += -DFROGGERS_VARIANT_GUITAR

include ../mk/daisy.mk
