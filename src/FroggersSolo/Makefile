TARGET := FroggersSolo
SRCS := FroggersSolo.cpp

# Selects the reverb-page build. Set before the include: daisy.mk composes
# DEFS immediately.
DEFS += -DFROGGERS_VARIANT_SOLO

include ../mk/daisy.mk
